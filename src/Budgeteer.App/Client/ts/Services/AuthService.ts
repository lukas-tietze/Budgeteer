/**
 * Verwaltet den Login-Status des Nutzers.
 */
export class AuthService {
  /**
   * Initialisiert eine neue Instanz der Klasse.
   */
  constructor() {}

  /**
   * Holt einen Wert, der angibt, ob aktuell ein Nutzer eingeloggt ist oder nicht.
   *
   * @returns `true`, wenn gerade ein Nutzer eingeloggt ist, sonst `false`.
   */
  public isLoggedIn(): boolean {
    return false;
  }
}
